<div class="row">
    <div class="col-2">
    </div>
    <!-- INSTEAD OF HAVING THESE EMPTY DIVS ^, YOU CAN USE OFFSET CLASSES IN BOOTSTRAP -->
    <%= form_for @comment, url: update_comment_path({id: ___, comment_id: ___}), do |f| %>
      <div class="form-group row">
        <%= f.label :content, class:"col-form-label col-sm-5" %>
        <%= f.text_area :content, class: "form-control col-sm-7", size: "60x15" %>
      </div>

      <div class="form-group row">
        <div class="col-sm-5">
        </div> 
        <!-- INSTEAD OF HAVING THESE EMPTY DIVS ^, YOU CAN USE OFFSET CLASSES IN BOOTSTRAP -->
        <%= f.submit "Submit", class:"btn btn-primary" %>
      </div>
    <% end %>
  </div>
